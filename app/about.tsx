import React from "react";
import { View } from "react-native";

const About = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        flexDirection: "row",
        alignItems: "center",
      }}
    >
      About Stack
    </View>
  );
};

export default About;
